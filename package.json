{
  "name": "senseease-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:integrity": "node tools/check_demo_integrity.mjs",
    "verify:demo": "npm run build && node tools/check_demo_integrity.mjs --require-dist"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.16"
  }
}
