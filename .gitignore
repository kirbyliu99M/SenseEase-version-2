# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Python bytecode (from local OpenVINO bridge tooling)
__pycache__/
*.pyc

# Large video files — hosted externally (Google Drive)
*.mp4

# OpenVINO IR weights are tracked via Git LFS (see .gitattributes), not
# ignored. Booth machines that clone the repo get the models out of the box.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Intel GPU compute kernel error dumps (OpenVINO compile failures on iGPU)
kernel.errors.txt
